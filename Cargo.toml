[package]
name = "ndir"
version = "0.1.0"
edition = "2021"
description = "Inline arrow-key directory navigation for your shell"
license = "MIT"

[dependencies]
crossterm = "0.28"
fuzzy-matcher = "0.3"

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
